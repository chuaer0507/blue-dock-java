# 日历 — 数据

无 `task_calendars` 表。事件 = 可见任务的 `start_at` / `end_at`（及全天约定）。

| 字段 | 用途 |
| ---- | ---- |
| start_at / end_at | 时间块；拖动改期 → `task/update` |
| 全天 | 由客户端约定（如仅日期或 flag；实现时与任务更新字段对齐） |

过滤：项目、成员、优先级等在查询任务列表后客户端或服务端筛选。
