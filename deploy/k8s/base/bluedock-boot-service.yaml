apiVersion: v1
kind: Service
metadata:
  name: bluedock-boot
  labels:
    app: bluedock-boot
    app.kubernetes.io/part-of: bluedock
spec:
  selector:
    app: bluedock-boot
  ports:
    - name: http
      port: 8080
      targetPort: http
  type: ClusterIP
